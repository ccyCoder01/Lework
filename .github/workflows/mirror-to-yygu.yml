name: Mirror to git.yygu.cn

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'release*'
    tags:
      - '*'

permissions:
  contents: read

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push mirror
        env:
          YYGU_TOKEN: ${{ secrets.YYGU_TOKEN }}
        run: |
          echo "::group::configure mirror remote"
          git remote add mirror "https://oauth2:${YYGU_TOKEN}@git.yygu.cn/corekg/lework.git"
          echo "::endgroup::"

          echo "[0/4] fetching all branches & tags from origin ..."
          git fetch origin '+refs/heads/*:refs/remotes/origin/*' '+refs/tags/*:refs/tags/*'
          echo "  exit=$?"

          echo "[1/4] pushing all branches ..."
          timeout 180 git push --force mirror 'refs/remotes/origin/*:refs/heads/*'; echo "  exit=$?"

          echo "[2/4] pushing all tags ..."
          timeout 180 git push --force --tags mirror; echo "  exit=$?"

          echo "[3/4] pruning branches removed upstream ..."
          timeout 180 git push --force mirror --prune 'refs/remotes/origin/*:refs/heads/*'; echo "  exit=$?"
